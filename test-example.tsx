const Test = () => {
  return (
    <div>
      Test
      {/* zkdn */}
      Liste des fonctionnalités: - Mettre en vérifi le produit du vendeur (fait)
      {/* L'admin */}
      -L'admin doit pourvoir traiter le produit: - Envoyer un livreur avec des
      infos du produit et du vendeur (fait)
      {/* livreur */}
      -reçevoir notif qu'il doit récuperer un colis -Le livreur pars récuperer
      'attends bérenger' le colis et confirmant le vendeur -confimer produit
      reçu et conforme (api fait) -Confirmer la mise en vente du produit
      {/* acheteur */}
      -Achete un produit (fait mais doit être encore vérifier)
      {/* L'admin  */}
      -Confie le livreur avec les infos du produits a livré - doit aller
      récupérer le produit et le livrer -reçois notif de quand il peut être
      livré
      {/* vendeur */}
      -reçois notif que son produit a été vendus -reçois l'argent (pas encore
      fait)
      {/* Partie de code à faire */}
      page des livreurs nav -logo -
    </div>
  );
};
export default Test;
